# Web Resources

A collection of web resources for use in my other projects. These files are hosted with GitHub Pages at [src.kaysting.dev](https://src.kaysting.dev).

- The `lib` folder contains minified copies of third-party client-side libraries
- The `fonts` folder contains font binaries and `.css` files to load them
-
